<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.brushing.member.mapper.OrderLotteryPrizeMapper">
    
    <resultMap type="OrderLotteryPrize" id="OrderLotteryPrizeResult">
        <result property="id"    column="id"    />
        <result property="configId"    column="config_id"    />
        <result property="amount"    column="amount"    />
        <result property="totalCount"    column="total_count"    />
        <result property="remainCount"    column="remain_count"    />
        <result property="probability"    column="probability"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectOrderLotteryPrizeVo">
        select id, config_id, amount, total_count, remain_count, probability, create_time from order_lottery_prize
    </sql>

    <select id="selectOrderLotteryPrizeList" parameterType="OrderLotteryPrize" resultMap="OrderLotteryPrizeResult">
        <include refid="selectOrderLotteryPrizeVo"/>
        <where>  
        </where>
    </select>
    
    <select id="selectOrderLotteryPrizeById" parameterType="Long" resultMap="OrderLotteryPrizeResult">
        <include refid="selectOrderLotteryPrizeVo"/>
        where id = #{id}
    </select>

    <insert id="insertOrderLotteryPrize" parameterType="OrderLotteryPrize" useGeneratedKeys="true" keyProperty="id">
        insert into order_lottery_prize
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="configId != null">config_id,</if>
            <if test="amount != null">amount,</if>
            <if test="totalCount != null">total_count,</if>
            <if test="remainCount != null">remain_count,</if>
            <if test="probability != null">probability,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="configId != null">#{configId},</if>
            <if test="amount != null">#{amount},</if>
            <if test="totalCount != null">#{totalCount},</if>
            <if test="remainCount != null">#{remainCount},</if>
            <if test="probability != null">#{probability},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateOrderLotteryPrize" parameterType="OrderLotteryPrize">
        update order_lottery_prize
        <trim prefix="SET" suffixOverrides=",">
            <if test="configId != null">config_id = #{configId},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="totalCount != null">total_count = #{totalCount},</if>
            <if test="remainCount != null">remain_count = #{remainCount},</if>
            <if test="probability != null">probability = #{probability},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteOrderLotteryPrizeById" parameterType="Long">
        delete from order_lottery_prize where id = #{id}
    </delete>

    <delete id="deleteOrderLotteryPrizeByIds" parameterType="String">
        delete from order_lottery_prize where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteOrderLotteryPrizeByConfigId" parameterType="Long">
        delete from order_lottery_prize where config_id = #{configId}
    </delete>
</mapper>
