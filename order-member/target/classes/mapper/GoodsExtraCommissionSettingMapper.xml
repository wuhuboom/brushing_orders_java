<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.order.member.mapper.GoodsExtraCommissionSettingMapper">
    
    <resultMap type="GoodsExtraCommissionSetting" id="GoodsExtraCommissionSettingResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="orderCount"    column="order_count"    />
        <result property="productPrice"    column="product_price"    />
        <result property="amount"    column="amount"    />
        <result property="isLocked"    column="is_locked"    />
        <result property="status"    column="status"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectGoodsExtraCommissionSettingVo">
        select id, user_id, order_count, product_price, amount, is_locked, status, create_time from goods_extra_commission_setting
    </sql>

    <select id="selectGoodsExtraCommissionSettingList" parameterType="GoodsExtraCommissionSetting" resultMap="GoodsExtraCommissionSettingResult">
        <include refid="selectGoodsExtraCommissionSettingVo"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="orderCount != null "> and order_count = #{orderCount}</if>
            <if test="productPrice != null "> and product_price = #{productPrice}</if>
            <if test="amount != null "> and amount = #{amount}</if>
            <if test="isLocked != null  and isLocked != ''"> and is_locked = #{isLocked}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectGoodsExtraCommissionSettingById" parameterType="Long" resultMap="GoodsExtraCommissionSettingResult">
        <include refid="selectGoodsExtraCommissionSettingVo"/>
        where id = #{id}
    </select>

    <insert id="insertGoodsExtraCommissionSetting" parameterType="GoodsExtraCommissionSetting" useGeneratedKeys="true" keyProperty="id">
        insert into goods_extra_commission_setting
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="orderCount != null">order_count,</if>
            <if test="productPrice != null">product_price,</if>
            <if test="amount != null">amount,</if>
            <if test="isLocked != null and isLocked != ''">is_locked,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="orderCount != null">#{orderCount},</if>
            <if test="productPrice != null">#{productPrice},</if>
            <if test="amount != null">#{amount},</if>
            <if test="isLocked != null and isLocked != ''">#{isLocked},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateGoodsExtraCommissionSetting" parameterType="GoodsExtraCommissionSetting">
        update goods_extra_commission_setting
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="orderCount != null">order_count = #{orderCount},</if>
            <if test="productPrice != null">product_price = #{productPrice},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="isLocked != null and isLocked != ''">is_locked = #{isLocked},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteGoodsExtraCommissionSettingById" parameterType="Long">
        delete from goods_extra_commission_setting where id = #{id}
    </delete>

    <delete id="deleteGoodsExtraCommissionSettingByIds" parameterType="String">
        delete from goods_extra_commission_setting where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
